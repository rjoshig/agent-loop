# Required: Anthropic API key for Claude.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model. Defaults to the value in src/config.js.
# ANTHROPIC_MODEL=claude-sonnet-4-5

# Optional: skip the interactive human gate (auto-approve). Useful for demos.
# AUTO_APPROVE_HUMAN_GATE=false
